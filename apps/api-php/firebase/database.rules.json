{
  "rules": {
    ".read": false,
    ".write": false,
    "channels": {
      "admin": {
        ".read": "auth != null && auth.token.kind === 'admin'"
      },
      "users": {
        "$uid": {
          ".read": "auth != null && auth.uid === $uid"
        }
      }
    }
  }
}
