import{DatePipe}from'@angular/common';import{ChangeDetectionStrategy,Component,OnInit,signal}from'@angular/core';import{FormControl,FormGroup,ReactiveFormsModule}from'@angular/forms';import{RouterLink}from'@angular/router';import{ApiService}from'../../core/api.service';import{IncidentPage}from'../../core/models';
@Component({standalone:true,imports:[DatePipe,ReactiveFormsModule,RouterLink],templateUrl:'./incidents.html',styleUrl:'./incidents.scss',changeDetection:ChangeDetectionStrategy.OnPush})
export class IncidentsComponent implements OnInit{readonly loading=signal(true);readonly error=signal('');readonly result=signal<IncidentPage|null>(null);readonly filters=new FormGroup({keyword:new FormControl('',{nonNullable:true}),status:new FormControl('',{nonNullable:true}),type:new FormControl('',{nonNullable:true})});constructor(private readonly api:ApiService){}ngOnInit(){this.load()}load(page=1){this.loading.set(true);this.error.set('');const value=this.filters.getRawValue();const query=Object.fromEntries(Object.entries({...value,page:String(page)}).filter(([,v])=>v));this.api.get<IncidentPage>('admin/incidents',query).subscribe({next:r=>{this.result.set(r);this.loading.set(false)},error:()=>{this.error.set('ไม่สามารถโหลดรายการแจ้งเหตุได้');this.loading.set(false)}})}}
