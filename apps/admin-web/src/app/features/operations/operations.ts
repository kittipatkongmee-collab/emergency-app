import{DatePipe}from'@angular/common';import{ChangeDetectionStrategy,Component,OnInit,signal}from'@angular/core';import{FormControl,FormGroup,ReactiveFormsModule,Validators}from'@angular/forms';import{DomSanitizer,SafeResourceUrl}from'@angular/platform-browser';import{RouterLink}from'@angular/router';import{ApiService}from'../../core/api.service';import{Incident}from'../../core/models';import{environment}from'../../../environments/environment';
interface AdminUser{id:string;username:string;fullName:string;role:string;status:string}interface Notice{id:string;title:string;message:string;createdAt:string;isRead:boolean}interface Audit{id:string;createdAt:string;action:string;entityType:string;entityId?:string;adminUser?:{fullName:string}}
@Component({standalone:true,imports:[RouterLink],template:`<h1 class="page-title">แผนที่เหตุการณ์</h1><p class="muted">ภาพรวมตำแหน่งเหตุการณ์ล่าสุด</p><section class="map-layout"><iframe title="แผนที่เหตุการณ์" [src]="mapUrl"></iframe><div class="card locations"><h2>จุดเกิดเหตุล่าสุด</h2>@if(loading()){<div class="state"><div class="spinner"></div></div>}@for(i of incidents();track i.id){<a [routerLink]="['/incidents',i.id]"><b>{{i.caseCode}}</b><span>{{i.address}}</span><em class="status">{{i.status}}</em></a>}</div></section>`,styles:[`.map-layout{display:grid;grid-template-columns:1fr 360px;gap:18px;margin-top:20px}.map-layout iframe{width:100%;height:680px;border:0;border-radius:var(--radius);box-shadow:var(--shadow)}.locations{padding:18px;align-self:start}.locations h2{margin-top:0;color:var(--primary-dark)}.locations a{display:grid;gap:3px;padding:14px 0;border-bottom:1px solid var(--border-color)}.locations span{font-size:.85rem;color:var(--text-secondary)}.locations em{justify-self:start}@media(max-width:900px){.map-layout{grid-template-columns:1fr}.map-layout iframe{height:450px}}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class IncidentMapComponent implements OnInit{readonly incidents=signal<Incident[]>([]);readonly loading=signal(true);readonly mapUrl:SafeResourceUrl;constructor(private readonly api:ApiService,sanitizer:DomSanitizer){this.mapUrl=sanitizer.bypassSecurityTrustResourceUrl(`${environment.mapEmbedBaseUrl}?bbox=100.35%2C13.55%2C100.90%2C14.10&layer=mapnik`)}ngOnInit(){this.api.get<Incident[]>('admin/dashboard/recent-incidents').subscribe({next:r=>{this.incidents.set(r);this.loading.set(false)},error:()=>this.loading.set(false)})}}
@Component({standalone:true,template:`<h1 class="page-title">จัดการผู้ใช้งาน</h1><p class="muted">บัญชีเจ้าหน้าที่และสิทธิ์การเข้าถึง</p><section class="card operation-card">@if(loading()){<div class="state"><div class="spinner"></div></div>}@else if(!users().length){<div class="state"><h2>ไม่พบผู้ใช้งาน</h2></div>}@else{<table><thead><tr><th>ชื่อผู้ใช้</th><th>ชื่อ-นามสกุล</th><th>บทบาท</th><th>สถานะ</th></tr></thead><tbody>@for(u of users();track u.id){<tr><td>{{u.username}}</td><td><b>{{u.fullName}}</b></td><td>{{u.role}}</td><td><span class="status">{{u.status}}</span></td></tr>}</tbody></table>}</section>`,styleUrl:'./operations.scss',changeDetection:ChangeDetectionStrategy.OnPush})
export class UsersComponent implements OnInit{readonly users=signal<AdminUser[]>([]);readonly loading=signal(true);constructor(private readonly api:ApiService){}ngOnInit(){this.api.get<AdminUser[]>('admin/users').subscribe({next:r=>{this.users.set(r);this.loading.set(false)},error:()=>this.loading.set(false)})}}
@Component({standalone:true,imports:[DatePipe],template:`<h1 class="page-title">ศูนย์การแจ้งเตือน</h1><p class="muted">เหตุการณ์และข้อความที่ต้องดำเนินการ</p><section class="card notification-list">@if(loading()){<div class="state"><div class="spinner"></div></div>}@else if(!items().length){<div class="state"><h2>ไม่มีการแจ้งเตือน</h2><p class="muted">คุณอ่านการแจ้งเตือนครบแล้ว</p></div>}@for(n of items();track n.id){<article [class.unread]="!n.isRead"><i>!</i><div><b>{{n.title}}</b><p>{{n.message}}</p><time>{{n.createdAt|date:'d MMM y HH:mm':'':'th'}}</time></div></article>}</section>`,styleUrl:'./operations.scss',changeDetection:ChangeDetectionStrategy.OnPush})
export class NotificationsComponent implements OnInit{readonly items=signal<Notice[]>([]);readonly loading=signal(true);constructor(private readonly api:ApiService){}ngOnInit(){this.api.get<Notice[]>('notifications').subscribe({next:r=>{this.items.set(r);this.loading.set(false)},error:()=>this.loading.set(false)})}}
@Component({standalone:true,imports:[DatePipe],template:`<h1 class="page-title">ประวัติการดำเนินการ</h1><p class="muted">Audit Log สำหรับการตรวจสอบย้อนหลัง</p><section class="card operation-card">@if(loading()){<div class="state"><div class="spinner"></div></div>}@else if(!items().length){<div class="state"><h2>ยังไม่มีประวัติ</h2></div>}@else{<table><thead><tr><th>วันเวลา</th><th>ผู้ดำเนินการ</th><th>การกระทำ</th><th>ข้อมูล</th></tr></thead><tbody>@for(a of items();track a.id){<tr><td>{{a.createdAt|date:'d MMM y HH:mm':'':'th'}}</td><td>{{a.adminUser?.fullName||'ระบบ'}}</td><td><b>{{a.action}}</b></td><td>{{a.entityType}} · {{a.entityId||'-'}}</td></tr>}</tbody></table>}</section>`,styleUrl:'./operations.scss',changeDetection:ChangeDetectionStrategy.OnPush})
export class AuditComponent implements OnInit{readonly items=signal<Audit[]>([]);readonly loading=signal(true);constructor(private readonly api:ApiService){}ngOnInit(){this.api.get<Audit[]>('admin/audit-logs').subscribe({next:r=>{this.items.set(r);this.loading.set(false)},error:()=>this.loading.set(false)})}}
@Component({standalone:true,imports:[ReactiveFormsModule],template:`<h1 class="page-title">ตั้งค่าระบบ</h1><p class="muted">ข้อมูลติดต่อและการปฏิบัติงานของศูนย์</p><form class="card settings" [formGroup]="form" (ngSubmit)="save()"><div class="field"><label>หมายเลขฉุกเฉิน</label><input formControlName="phone"></div><div class="field"><label>เวลาทำการ</label><input formControlName="hours"></div><div class="field"><label>ข้อความประกาศ</label><textarea rows="4" formControlName="announcement"></textarea></div><button class="btn-primary">{{saving()?'กำลังบันทึก…':'บันทึกการตั้งค่า'}}</button>@if(message()){<p role="status">{{message()}}</p>}</form>`,styleUrl:'./operations.scss',changeDetection:ChangeDetectionStrategy.OnPush})
export class SettingsComponent{readonly saving=signal(false);readonly message=signal('');readonly form=new FormGroup({phone:new FormControl('0 2509 1520',{nonNullable:true,validators:[Validators.required]}),hours:new FormControl('24 ชั่วโมง',{nonNullable:true}),announcement:new FormControl('',{nonNullable:true})});constructor(private readonly api:ApiService){}save(){if(this.form.invalid)return;this.saving.set(true);this.api.patch('admin/settings',{emergencyContact:this.form.getRawValue()}).subscribe({next:()=>{this.saving.set(false);this.message.set('บันทึกการตั้งค่าแล้ว')},error:()=>{this.saving.set(false);this.message.set('บันทึกไม่สำเร็จ')}})}}
@Component({standalone:true,imports:[RouterLink],template:`<h1 class="page-title">โปรไฟล์เจ้าหน้าที่</h1><section class="card profile"><span>สช</span><div><p class="eyebrow">บัญชีผู้ใช้งานปัจจุบัน</p><h2>ผู้ดูแลระบบ</h2><p class="muted">กองบินตำรวจ สำนักงานตำรวจแห่งชาติ</p><a routerLink="/change-password" class="btn-outline">เปลี่ยนรหัสผ่าน</a></div></section>`,styleUrl:'./operations.scss',changeDetection:ChangeDetectionStrategy.OnPush})export class ProfileComponent{}
@Component({standalone:true,imports:[ReactiveFormsModule],template:`<h1 class="page-title">เปลี่ยนรหัสผ่าน</h1><form class="card settings" [formGroup]="form" (ngSubmit)="save()"><div class="field"><label>รหัสผ่านปัจจุบัน</label><input type="password" formControlName="currentPassword"></div><div class="field"><label>รหัสผ่านใหม่</label><input type="password" formControlName="newPassword"><small class="muted">อย่างน้อย 12 ตัวอักษร</small></div><button class="btn-primary">บันทึกรหัสผ่านใหม่</button>@if(message()){<p>{{message()}}</p>}</form>`,styleUrl:'./operations.scss',changeDetection:ChangeDetectionStrategy.OnPush})
export class ChangePasswordComponent{readonly message=signal('');readonly form=new FormGroup({currentPassword:new FormControl('',{nonNullable:true,validators:[Validators.required]}),newPassword:new FormControl('',{nonNullable:true,validators:[Validators.required,Validators.minLength(12)]})});constructor(private readonly api:ApiService){}save(){if(this.form.invalid){this.form.markAllAsTouched();return}this.api.patch('admin/auth/change-password',this.form.getRawValue()).subscribe({next:()=>this.message.set('เปลี่ยนรหัสผ่านสำเร็จ'),error:()=>this.message.set('เปลี่ยนรหัสผ่านไม่สำเร็จ')})}}
