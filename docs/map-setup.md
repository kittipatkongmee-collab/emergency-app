# Map setup

Create platform-restricted Google Maps keys for Android, iOS and web. Pass the key through environment/build configuration and restrict APIs to Maps SDK plus geocoding services actually used. Coordinates remain editable and are persisted even if reverse geocoding is temporarily unavailable.
