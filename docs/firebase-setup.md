# Firebase setup

Create Android and iOS apps in the production Firebase project. Keep `google-services.json` and `GoogleService-Info.plist` outside the public repository workflow. Configure the server service account through `FCM_PROJECT_ID`, `FCM_CLIENT_EMAIL` and multiline `FCM_PRIVATE_KEY`, then register device tokens through `/api/v1/devices/register`.
